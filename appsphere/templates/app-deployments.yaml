{{- range .Values.apps }}
{{- if .app }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .name }}
  namespace: {{ .namespace | default $.Values.global.namespace }}
  labels:
    {{- include "appshpere.labels" $ | nindent 4 }}
    app: {{ .app }}
spec:
  replicas: {{ .replicas | default 1 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: {{ .app }}
  template:
    metadata:
      labels:
        app: {{ .app }}
        app.kubernetes.io/name: {{ .name }}-pod
    spec:
      containers:
        - name: {{ .name }}
          image: "{{ $.Values.global.image.registry }}/{{ $.Values.global.image.repository }}/{{ .imageName }}:{{ .tag }}"
          imagePullPolicy: {{ $.Values.global.image.pullPolicy }}
          {{- if .command }}
          command: [{{- range $index, $elem := .command }}{{ if $index }}, {{ end }}"{{ $elem }}"{{- end }}]
          {{- end }}
          {{- if .ports }}
          ports:
            - containerPort: {{ .ports.targetPort | default .ports.port }}
              name: http-port
              protocol: TCP
            {{- if .ports.grpcPort }}
            - containerPort: {{ .ports.grpcPort }}
              name: grpc-port
              protocol: TCP
            {{- end }}
          {{- end }}
          {{- if .env }}
          env:
            {{- toYaml .env | nindent 12 }}
          {{- end }}
          {{- if .envFrom }}
          envFrom:
            - secretRef:
                name: {{ .envFrom }}
          {{- end }}
          {{- if .resources.enabled }}
          resources:
            {{- if .resources.requests }}
            {{- toYaml .resources.requests | nindent 12 }}
            {{- end }}
            {{- if .resources.limits }}
            {{- toYaml .resources.limits | nindent 12 }}
            {{- end }}
          {{- end }}
          {{- if .startupProbe.enabled }}
          startupProbe:
            exec:
              command: ["true"]
            initialDelaySeconds: {{ .startupProbe.initialDelaySeconds | default 60 }}
            periodSeconds: {{ .startupProbe.periodSeconds | default 10 }}
          {{- end }}
      imagePullSecrets:
        {{- toYaml $.Values.global.imagePullSecrets | nindent 8 }}
{{- end }}
{{- end }}